import type { SpecializationGroup } from '../../types';

// Legacy hardcoded data is intentionally disabled. Runtime specialization logic
// now comes only from the track-specific JSON files under files/קבוצות התמחות.
export const eeSpecializations: SpecializationGroup[] = [];
